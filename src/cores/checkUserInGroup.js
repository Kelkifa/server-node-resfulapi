


/**
 * 
 * @param {object} model mongodb model
 * @param {string} modelId model id
 * @param {string} userId user id
 * @param {boolean} isAlowDemo alow demo
 * @return {boolean} true : user is in group, false: user is not in group
 */
function checkUserInGroup(model, modelId, userId, isAlowDemo) {

    // const result = model.findOne()
    return true;

}

module.exports = checkUserInGroup;